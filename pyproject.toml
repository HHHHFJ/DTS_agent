[project]
name = "dts-agent"
version = "0.1.0"
description = "Local OpenCode DTS security knowledge base and review workflow"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
dts-agent = "dts_agent.cli:main"

[tool.unittest]
start-dir = "tests"
