---
description: Query the DTS security knowledge base
agent: dts-guardian
---

Query the DTS security knowledge base with `dts_kb_query` using `$ARGUMENTS` as the query text. Return the most relevant historical issues with ticket id, issue type, vulnerable snippet, fixed snippet, recommendation, and confidence.
